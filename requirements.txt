# Host-native convenience install (current OS only). Prefer fetch_vendor.py
# for release packaging (tagged vendor/macosx_* and vendor/win_amd64 trees).
#
# Local Anki Python example:
#   /path/to/AnkiProgramFiles/.venv/bin/python -m pip install -r requirements.txt -t vendor
-r requirements-common.txt
rumps>=0.4.0; sys_platform == "darwin"
pyobjc-framework-Cocoa>=10.0; sys_platform == "darwin"
pystray>=0.19.0; sys_platform == "win32"
six>=1.9; sys_platform == "win32"
winrt-runtime>=3.0; sys_platform == "win32"
winrt-Windows.Foundation>=3.0; sys_platform == "win32"
winrt-Windows.Foundation.Collections>=3.0; sys_platform == "win32"
winrt-Windows.Media.Control>=3.0; sys_platform == "win32"
